[project]
name = "parlez-e2e"
version = "0.1.0"
description = "On-demand EC2 instance for parlez e2e testing"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-aws>=7.0.0,<8.0.0",
]

[tool.uv]
package = false