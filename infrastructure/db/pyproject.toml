[project]
name = "parlez-db"
version = "0.1.0"
description = "Persistent Postgres EC2 instance for parlez"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-aws>=7.0.0,<8.0.0",
]

[tool.uv]
package = false